{
  "name": "alvaro-analytics",
  "version": "1.0.0",
  "description": "Alvaro Analytics — Plateforme de prédiction de performance académique",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "npx concurrently --kill-others \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:api": "cd backend && npm run dev",
    "dev:ui": "cd frontend && npm run dev",
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
